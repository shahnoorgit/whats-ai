import Image from "next/image";

const Custombtn = () => {
  return (
    <button
      disabled={false}
      type={"button"}
      className={`custom-btn`}
      onClick={() => {}}
    >
      Custombtn
    </button>
  );
};

export default Custombtn;
